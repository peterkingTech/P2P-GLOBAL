# Next.js build output — never committed, regenerated by `next build`/`next dev`.
/.next/
/out/

# Dependencies (also covered by the monorepo root .gitignore).
/node_modules/

# Local env files.
.env*.local

# Misc.
.DS_Store
*.tsbuildinfo
