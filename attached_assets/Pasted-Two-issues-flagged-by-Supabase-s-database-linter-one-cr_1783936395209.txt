Two issues flagged by Supabase's database linter, one critical, one performance-related but widespread — fix both.

1. CRITICAL — SECURITY DEFINER VIEW: public.forest_nodes is defined with SECURITY DEFINER, meaning it runs with the view creator's permissions rather than the querying user's, bypassing RLS. Investigate why this was set up this way (likely to let the Global Forest world-map feature read across multiple users' data). Determine whether SECURITY DEFINER is genuinely necessary here, and if so, add explicit, careful filtering INSIDE the view definition itself to ensure it only exposes exactly the fields/rows appropriate for the Global Forest feature (e.g. aggregate/public-safe data only, never full profile details) — do not leave it as a blanket bypass. If SECURITY DEFINER isn't actually necessary, convert it to a normal view (SECURITY INVOKER, the default) so real RLS applies.

2. PERFORMANCE — Auth RLS Initialization Plan warnings, present across many tables (p2p_registration_profiles, p2p_profiles, p2p_lesson_progress, p2p_assignment_submissions, p2p_lesson_evaluations, and others per the linter's full list). Every RLS policy calling auth.uid() or auth.jwt() directly should be rewritten to wrap it as (select auth.uid()) / (select auth.jwt()) instead, so Postgres evaluates it once per query rather than once per row. Go through every flagged table systematically — don't just fix a few and stop, since the linter is showing this repeated across most of the app's tables.

Report back: for #1, confirm what the view actually exposes now and whether it required a full rewrite or a full conversion to a normal view. For #2, give me a complete list of every table/policy you updated, and confirm zero "Auth RLS Initialization Plan" warnings remain by re-running Supabase's linter (Database → Advisors, or wherever this panel lives) after the fix.

TEST WITH A GENUINELY FRESH ACCOUNT, NOT THE EXISTING ADMIN/TEST ACCOUNT, BEFORE REPORTING DONE — specifically confirm the Global Forest feature still works correctly after the forest_nodes fix, since this is the highest-risk change in this prompt.