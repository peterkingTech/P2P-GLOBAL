1. Language selection on the very first screen — reasonable, real UX improvement:
Add a language selection step to the very first screen a new user sees (before or as part of onboarding, prior to registration) — let them set both App Language and Content Language immediately, not buried in Settings after signup.
2 & 5. Progress percentage desync AND lesson not unlocking after approval — same root cause, found it precisely
I traced this exactly: there's an old function, markLessonComplete(), still live in DataContext.tsx, that writes completed: true directly and unconditionally to p2p_lesson_progress — completely bypassing the evaluation system we built. This is almost certainly still wired to whatever button we asked to be renamed "I've Read This" a few messages back — meaning the rename may have changed the label without changing the underlying function it calls. Two different code paths are now setting lesson completion with two different sets of rules, which explains both bugs at once:
Critical bug: markLessonComplete() in DataContext.tsx (around line 1897) still writes completed: true directly to p2p_lesson_progress, completely bypassing the peer evaluation gate — this conflicts with the evaluation-approval flow that's supposed to be the ONLY thing that sets lesson completion.

1. Find every UI element that currently calls markLessonComplete() and confirm: is this the button we asked to be renamed "I've Read This"? If so, it was renamed in label only — the underlying function still marks real completion, which it should NOT do per our earlier design (reading-complete should be a separate, low-stakes tracker with zero effect on progress/points/unlocking).
2. Split this into two genuinely separate functions: one for "I've read this" (writes to a simple reading-tracked flag only, no effect on lesson_progress.completed), and confirm the REAL completion path is exclusively the evaluation-approval trigger from our recent work.
3. Fix the overall progress percentage calculation to only count lessons where p2p_lesson_progress.completed = true via the APPROVED evaluation path — audit every place "completedLessons" is calculated (learn.tsx and any module-summary logic) for the same bug.
4. Fix lesson unlocking: confirm the next-lesson-unlock check reads from the correct, single source of truth (approval status), not the old markLessonComplete() write path.

TEST WITH A GENUINELY FRESH ACCOUNT, NOT THE EXISTING ADMIN/TEST ACCOUNT, BEFORE REPORTING DONE — submit a lesson, confirm it does NOT count toward percentage or unlock the next lesson until actually approved by an evaluator, then approve it and confirm both update correctly and immediately.
3. Translation completeness — covered above in the language-mismatch fix.
4. "Called in: Student" for everyone — confirmed, and this is exactly why your Mission/Calling/Occupation redesign is the right fix, not a patch
I found the exact line: the profile screen literally displays profile.role (the permissions field — student/peer_guide/church_leader/etc.) as if it were someone's personal calling. Every new user defaults to role = 'student' for access-control reasons, so of course everyone sees the same label — this was never meant to represent identity, only permission level. Your redesign correctly separates these concerns. I'll fold this into the full Mission/Calling/Occupation prompt below.